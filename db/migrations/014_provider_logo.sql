-- ═══════════════════════════════════════════════════════════════════
--  Логотип провайдера инфраструктуры.
--
--  Хостера узнают по значку быстрее, чем по названию. Ходить за
--  фавиконкой на чужой сервис нельзя: панель self-hosted, и такой
--  запрос из браузера администратора рассказал бы стороннему сервису,
--  какие хостеры у него используются. Поэтому адрес задаёт владелец,
--  а без него рисуем монограмму.
-- ═══════════════════════════════════════════════════════════════════

ALTER TABLE infra_providers
    ADD COLUMN IF NOT EXISTS logo_url text;
